targetted_decisions = {
	2makeconduit = {
		
		ai_target_filter  = dynasty
		filter = sub_realm
	
		from_potential = {
			is_playable = yes
			NOT = { trait = incapable }
			trait = Sorcery
			ai = no
			}
	
		potential = {
		NOT = { trait = Conduit } 
		NOT = { character = FROM}
		}
		
		allow = { at_location = FROM }
		
		effect = {	
		add_trait = Conduit
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
		}
	}
	}