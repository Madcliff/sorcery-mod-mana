title_decisions = {
	magic_blessings_descison = {
				filter = realm_owned
		ai_target_filter  = realm_owned
	
		from_potential = {
			is_playable = yes
			ai = no			
			NOT = {	OR = { trait = incapable has_character_flag = sorcery } }
			trait = Sorcery 
			check_variable = { which = "magic_power" value = 1 }
			}
	
		potential = {
		tier = COUNT
		}
		
		allow = { 		 
		}
		
		effect = {	
			location = { 	
				save_event_target_as = blessingsloc	
			}
		hidden_tooltip = { character_event = { id = blessings.1  } }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
		}
	}	
}