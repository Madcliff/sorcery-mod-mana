targetted_decisions = {
	2immortality_gift_decision = {
			
		ai_target_filter  = sub_realm
		filter = all
	
		from_potential = {
		ai = no		
			any_realm_character = { 
				at_location = ROOT 
				has_character_flag = elixer_of_life 
				NOT = {	
					trait = incapable 
				}
				trait = Alchemist 
			}
		}
	
		potential = {
			NOT = {
				trait = Undying
			}
		}
		
		allow = { 		
				FROM = { 
					any_realm_character = { 
						trait = Alchemist 
						at_location = ROOT 
						has_character_flag = elixer_of_life  
					}	
				}
			}
		
		effect = {	
			character_event = { id = immortalspell.1 } 
			character_event = { id = immortalspell.2 years = 50 } 
			FROM = { 					
				any_realm_character = {
					limit = { has_character_flag = elixer_of_life at_location = ROOT }
					clr_character_flag = elixer_of_life
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
		}
	}
	}