targetted_decisions = {
	premonitions_decision = {
	filter = self
	ai_target_filter  = self
	
		from_potential = {
			check_variable = { which = "magic_power" value = 1 }
			is_playable = yes
			NOT = {	OR= { trait = incapable has_character_flag = sorcery}}
			trait = Sorcery
			ai = no
			}
	
		potential = {
		character = FROM 		
		}
		
		allow = {
		}
		
		effect = {	
			hidden_tooltip = { character_event = { id = premonitions.1 } }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}	
	}